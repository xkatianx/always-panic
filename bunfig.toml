[test]
coverage = true
coverageSkipTestFiles = true
# function threshold is below 1 because bun counts the implicit default
# constructor of every constructor-less class as a function that can never
# be hit, and TypedError subclasses must not declare constructors
coverageThreshold = { line = 1, function = 0.9 }
