node_modules
.cursor
AGENTS.md

# Generated HTML API docs (`bun run docs`); deployed to GitHub Pages by
# docs-pages.yml, which rebuilds them in CI. Never committed.
site

# Build output (`bun run build`); regenerated by `prepublishOnly` in the
# publish workflow, so the committed tree never needs it.
dist
